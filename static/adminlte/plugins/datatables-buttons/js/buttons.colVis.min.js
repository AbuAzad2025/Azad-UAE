/*!
 Column visibility buttons for Buttons and DataTables.
 2016 SpryMedia Ltd - datatables.net/license
*/
((h) => {
	"function" === typeof define && define.amd
		? define(["jquery", "datatables.net", "datatables.net-buttons"], (e) =>
				h(e, window, document),
			)
		: "object" === typeof exports
			? (module.exports = (e, g) => {
					e || (e = window);
					(g && g.fn.dataTable) || (g = require("datatables.net")(e, g).$);
					g.fn.dataTable.Buttons || require("datatables.net-buttons")(e, g);
					return h(g, e, e.document);
				})
			: h(jQuery, window, document);
})((h, e, g, l) => {
	e = h.fn.dataTable;
	h.extend(e.ext.buttons, {
		colvis: (b, a) => {
			var c = null,
				d = {
					extend: "collection",
					init: (f, k) => {
						c = k;
					},
					text: (f) => f.i18n("buttons.colvis", "Column visibility"),
					className: "buttons-colvis",
					closeButton: !1,
					buttons: [
						{
							extend: "columnsToggle",
							columns: a.columns,
							columnText: a.columnText,
						},
					],
				};
			b.on("column-reorder.dt" + a.namespace, (f, k, m) => {
				b.button(null, b.button(null, c).node()).collectionRebuild([
					{
						extend: "columnsToggle",
						columns: a.columns,
						columnText: a.columnText,
					},
				]);
			});
			return d;
		},
		columnsToggle: (b, a) =>
			b
				.columns(a.columns)
				.indexes()
				.map((c) => ({
					extend: "columnToggle",
					columns: c,
					columnText: a.columnText,
				}))
				.toArray(),
		columnToggle: (b, a) => ({
			extend: "columnVisibility",
			columns: a.columns,
			columnText: a.columnText,
		}),
		columnsVisibility: (b, a) =>
			b
				.columns(a.columns)
				.indexes()
				.map((c) => ({
					extend: "columnVisibility",
					columns: c,
					visibility: a.visibility,
					columnText: a.columnText,
				}))
				.toArray(),
		columnVisibility: {
			columns: l,
			text: (b, a, c) => c._columnText(b, c),
			className: "buttons-columnVisibility",
			action: (b, a, c, d) => {
				b = a.columns(d.columns);
				a = b.visible();
				b.visible(d.visibility !== l ? d.visibility : !(a.length && a[0]));
			},
			init: function (b, a, c) {
				a.attr("data-cv-idx", c.columns);
				b.on("column-visibility.dt" + c.namespace, (f, k) => {
					k.bDestroying ||
						k.nTable != b.settings()[0].nTable ||
						this.active(b.column(c.columns).visible());
				}).on("column-reorder.dt" + c.namespace, (f, k, m) => {
					c.destroying ||
						1 !== b.columns(c.columns).count() ||
						(this.text(c._columnText(b, c)),
						this.active(b.column(c.columns).visible()));
				});
				this.active(b.column(c.columns).visible());
			},
			destroy: (b, a, c) => {
				b.off("column-visibility.dt" + c.namespace).off(
					"column-reorder.dt" + c.namespace,
				);
			},
			_columnText: (b, a) => {
				var c = b.column(a.columns).index(),
					d = b.settings()[0].aoColumns[c].sTitle;
				d || (d = b.column(c).header().innerHTML);
				d = d
					.replace(/\n/g, " ")
					.replace(/<br\s*\/?>/gi, " ")
					.replace(/<select(.*?)<\/select>/g, "")
					.replace(/<!--.*?-->/g, "")
					.replace(/<.*?>/g, "")
					.replace(/^\s+|\s+$/g, "");
				return a.columnText ? a.columnText(b, c, d) : d;
			},
		},
		colvisRestore: {
			className: "buttons-colvisRestore",
			text: (b) => b.i18n("buttons.colvisRestore", "Restore visibility"),
			init: (b, a, c) => {
				c._visOriginal = b
					.columns()
					.indexes()
					.map((d) => b.column(d).visible())
					.toArray();
			},
			action: (b, a, c, d) => {
				a.columns().every(function (f) {
					f =
						a.colReorder && a.colReorder.transpose
							? a.colReorder.transpose(f, "toOriginal")
							: f;
					this.visible(d._visOriginal[f]);
				});
			},
		},
		colvisGroup: {
			className: "buttons-colvisGroup",
			action: (b, a, c, d) => {
				a.columns(d.show).visible(!0, !1);
				a.columns(d.hide).visible(!1, !1);
				a.columns.adjust();
			},
			show: [],
			hide: [],
		},
	});
	return e.Buttons;
});
