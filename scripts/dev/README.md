# Dev Scripts — سكريبتات التطوير

This folder is reserved for development, debugging, and analysis scripts.

**Do not run scripts in production without review.**

## Current Contents

*(Empty — no dev scripts currently stored.)*

## Adding New Scripts

- Place one-off dev scripts here.
- Name them descriptively (`audit_`, `check_`, `fix_`, `seed_`, etc.).
- Do not commit destructive scripts (wipe, purge, etc.) — use `scripts/maintenance/` for those with explicit warnings.
- Delete or move old scripts to archive once they are no longer needed.
