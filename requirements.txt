# =============================================================================
# Single source of truth for dependencies. Use ONE virtual environment only
# (e.g. .venv or venv). Install: python -m venv .venv && .venv\Scripts\activate
# then: pip install -r requirements.txt
# Dev/CI: pip install -r requirements.txt
# =============================================================================

# Core Framework
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
click==8.1.7
itsdangerous==2.2.0

# Database
SQLAlchemy==2.0.44
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
alembic>=1.18.0,<2
psycopg2-binary
redis==5.0.1

# Authentication & Security
Flask-Login==0.6.3
Flask-WTF==1.2.1
WTForms==3.1.1
bcrypt==4.2.1
cryptography>=42.0.0,<45
email-validator>=2.1.0.post1
Flask-Cors==6.0.2
Flask-Limiter==3.5.0

# Asynchronous & Tasks
Celery>=5.6.2,<6
kombu>=5.6.0,<6
Flask-SocketIO==5.3.6
python-socketio==5.14.0
python-engineio==4.13.2

# Payments & webhooks
stripe>=8.0.0,<13

# Optional integrations (enable when configured)
elasticsearch>=8.12.0,<9

# Utilities
python-dotenv==1.2.2
python-dateutil==2.9.0
pytz==2025.2
requests==2.33.0
colorama==0.4.6
Flask-Mail==0.9.1
Flask-Babel==4.0.0
Flask-Caching==2.1.0
Flask-Compress==1.14
Brotli>=1.0.9

# Data Processing & AI
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0
joblib>=1.3.0
# Reserve — Groq/OpenAI/Anthropic APIs are called via requests today; SDKs ready for migration:
openai>=1.0.0
anthropic>=0.3.0

# POS Hardware (thermal printer, ESC/POS) — reserve for direct ESC/POS integration
python-escpos>=3.1

# Reporting & Documents
# Reserve — PDF via WeasyPrint/HTML today; ReportLab available for programmatic PDFs
reportlab==4.0.7
weasyprint==68.1
openpyxl==3.1.2
# Reserve — Excel export currently uses openpyxl; xlsxwriter for performance-heavy exports
xlsxwriter==3.2.5
Pillow==12.2.0
qrcode[pil]>=8.2

# Server
gunicorn==22.0.0

# HTML Parsing (AI / scraping helpers)
beautifulsoup4==4.12.3

# GraphQL
graphene==3.3

# Testing (dev/CI)
pytest>=9.1.0
pytest-cov>=7.1.0
pytest-flask>=1.3.0
pytest-mock>=3.14
bandit>=1.7.9
tinycss2>=1.2.0

# Monitoring — reserve; enable in app when SENTRY_DSN / Prometheus exporter is wired
sentry-sdk==1.45.1
prometheus-flask-exporter==0.23.2
psutil==5.9.8

# Misc
packaging>=24.0,<26
certifi==2025.8.3
urllib3==2.7.0
six==1.16.0

# -----------------------------------------------------------------------------
# Deployment notes (not pip packages):
# - WeasyPrint needs system libs: cairo, pango, gdk-pixbuf (apt install in Docker)
# - bcrypt / Flask-Cors kept for compatibility; passwords use Werkzeug pbkdf2 today
# - elasticsearch above is optional — only when ELASTICSEARCH_URL is configured
# -----------------------------------------------------------------------------
